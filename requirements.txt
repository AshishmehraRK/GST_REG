# Core dependencies for GST automation
selenium==4.33.0
requests==2.32.4
python-dotenv==1.1.1

# Flask API dependencies
Flask==3.0.0
Flask-RESTX==1.3.0
Werkzeug==3.0.1

# Optional: OTP server dependencies (only if using otp_server.py)
flask-cors==5.0.0
redis==5.2.0
